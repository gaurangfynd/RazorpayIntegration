{
    "parameters": [
        {
            "name": "operation",
            "meta": {
                "displayName": "Operation",
                "displayType": "select",
                "placeholder": "Select an operation",
                "description": "Select the operation you want to perform.",
                "options": [
                    {
                        "label": "Fetch all settlements",
                        "value": "settlement.fetchSettlements",
                        "description": "Fetch details of all settlements."
                    },
                    {
                        "label": "Fetch settlement details by ID",
                        "value": "settlement.fetchSettlementBySettlementId",
                        "description": "Fetch settlement details with the help of settlement ID."
                    }
                ],
                "validation": {
                    "required": true
                },
                "dependencies": {
                    "conditions": [
                        {
                            "field": "resource",
                            "operator": "EQUALS",
                            "value": "settlement"
                        }
                    ]
                }
            }
        }
    ],
    "fetchSettlements": {
        "parameters": [],
        "definition": {
            "method": "GET",
            "url": "https://api.razorpay.com/v1/settlements",
            "headers": {
                "ContentType": "application/json",
                "Authorization": "Basic convertBufferBase64String({{secrets.client_id}}:{{secrets.client_secret}})"
            },
            "response": {
                "output": "{{response.data}}",
                "error": {
                    "message": "{{response.data.errors.message}}",
                    "code": "{{response.data.errors.code}}"
                }
            }
        }
    },
    "fetchSettlementBySettlementId": {
        "parameters": [
            {
                "name": "settlementId",
                "meta": {
                    "displayName": "Settlement ID",
                    "displayType": "text",
                    "placeholder": "Enter the settlement ID",
                    "description": "Enter the settlement ID for which you want to fetch settlement details.",
                    "validation": {
                        "required": true
                    }
                }
            }
        ],
        "definition": {
            "method": "GET",
            "url": "https://api.razorpay.com/v1/settlements/{{parameters.settlementId}}",
            "headers": {
                "ContentType": "application/json",
                "Authorization": "Basic convertBufferBase64String({{secrets.client_id}}:{{secrets.client_secret}})"
            },
            "response": {
                "output": "{{response.data}}",
                "error": {
                    "message": "{{response.data.errors.message}}",
                    "code": "{{response.data.errors.code}}"
                }
            }
        }
    }
}