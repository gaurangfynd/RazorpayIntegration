{
    "parameters": [
        {
            "meta": {
                "value": "access_token",
                "options": [
                    {
                        "label": "API Key (Basic Auth)",
                        "value": "access_token"
                    }
                ],
                "displayName": "Authentication Type",
                "displayType": "hidden"
            },
            "name": "type"
        }
    ],
    "access_token": {
        "token": {
            "url": "https://api.razorpay.com/v1/orders",
            "method": "GET",
            "headers": {
                "Content-Type": "application/json",
                "Authorization": "Basic convertBufferBase64String({{secrets.client_id}}:{{secrets.client_secret}})"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "validate": {
            "url": "https://api.razorpay.com/v1/orders",
            "method": "GET",
            "headers": {
                "Content-Type": "application/json",
                "Authorization": "Basic convertBufferBase64String({{secrets.client_id}}:{{secrets.client_secret}})"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "Client ID is required"
                        }
                    },
                    "description": "Razorpay Client ID used for Basic authentication.",
                    "displayName": "Client ID",
                    "displayType": "text",
                    "placeholder": "Enter Client ID (For Example: rzp_test_...)"
                },
                "name": "client_id"
            },
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "Client Secret is required"
                        }
                    },
                    "description": "Razorpay Client Secret used for Basic authentication.",
                    "displayName": "Client Secret",
                    "displayType": "password",
                    "placeholder": "Enter Client Secret"
                },
                "name": "client_secret"
            }
        ]
    }
}
